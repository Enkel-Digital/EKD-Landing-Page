{
  "name": "landing_page",
  "version": "1.0.0",
  "description": "Landing page for EKD",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build_devcon": "html-minifier --remove-comments --collapse-whitespace devcon.html -o index.html",
    "build_devbiz": "html-minifier --remove-comments --collapse-whitespace devbiz.html -o business.html",
    "build": "npm run build_devcon && npm run build_devbiz",
    "staging": "npm run build_devcon && npm run build_devbiz && mv index.html ../docs && mv business.html ../docs",
    "deploy": "npm run build && firebase deploy --only hosting"
  },
  "author": "Jaime Loeuf",
  "license": "MIT",
  "devDependencies": {
    "html-minifier": "^4.0.0"
  }
}
