{
  "name": "landing_page",
  "version": "1.0.0",
  "description": "Landing page for EKD",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "html-minifier --remove-comments --collapse-whitespace dev.html -o index.html",
    "deploy": "npm run build && firebase deploy --only hosting"
  },
  "author": "Jaime Loeuf",
  "license": "MIT",
  "dependencies": {
    "html-minifier": "^4.0.0"
  }
}
